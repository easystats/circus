# lmerMod_1

Model of class lmerMod

## Usage

``` r
lmerMod_1
```

## Format

An object of class `lmerMod` of length 1.

## Examples

``` r
if (FALSE) {
lme4::lmer(wt ~ cyl + (1 | gear), data = mtcars)
}
```

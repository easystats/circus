# brms_mixed_7

Model of class brmsfit

## Usage

``` r
brms_mixed_7
```

## Format

An object of class `brmsfit` of length 19.

## Examples

``` r
if (FALSE) {
# \dontrun{
brm(mpg ~ wt + (1 | cyl) + (1 + wt | gear), data = mtcars)
# }
}
```

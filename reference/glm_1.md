# glm_1

Model of class glm

## Usage

``` r
glm_1
```

## Format

An object of class `glm` (inherits from `lm`) of length 30.

## Examples

``` r
if (FALSE) {
glm(formula = vs ~ wt, family = "binomial", data = mtcars)
}
```

# anova_1

Model of class anova

## Usage

``` r
anova_1
```

## Format

An object of class `anova` (inherits from `data.frame`) with 2 rows and
5 columns.

## Examples

``` r
if (FALSE) {
anova(lm(Sepal.Width ~ Species, data = iris))
}
```

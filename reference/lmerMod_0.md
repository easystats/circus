# lmerMod_0

Model of class lmerMod

## Usage

``` r
lmerMod_0
```

## Format

An object of class `lmerMod` of length 1.

## Examples

``` r
if (FALSE) {
lme4::lmer(wt ~ 1 + (1 | gear), data = mtcars)
}
```

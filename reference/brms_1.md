# brms_1

Model of class brmsfit

## Usage

``` r
brms_1
```

## Format

An object of class `brmsfit` of length 19.

## Examples

``` r
if (FALSE) {
# \dontrun{
set.seed(333)
brms::brm(mpg ~ wt + cyl, data = mtcars)
# }
}
```

# aov_1

Model of class aov

## Usage

``` r
aov_1
```

## Format

An object of class `aov` (inherits from `lm`) of length 13.

## Examples

``` r
if (FALSE) {
aov(Sepal.Width ~ Species, data = iris)
}
```

# glm_0

Model of class glm

## Usage

``` r
glm_0
```

## Format

An object of class `glm` (inherits from `lm`) of length 30.

## Examples

``` r
if (FALSE) {
glm(formula = vs ~ 1, family = "binomial", data = mtcars)
}
```

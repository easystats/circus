# lm_5

Model of class lm

## Usage

``` r
lm_5
```

## Format

An object of class `lm` of length 12.

## Examples

``` r
if (FALSE) {
lm(formula = mpg ~ wt + poly(cyl, 2, raw = TRUE), data = mtcars)
}
```
